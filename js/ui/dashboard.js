
function updateDashboard(){

countStore(
"customers",
"customerCount"
);

countStore(
"devices",
"deviceCount"
);

countStore(
"repairs",
"repairCount"
);

countStore(
"products",
"productCount"
);

}
